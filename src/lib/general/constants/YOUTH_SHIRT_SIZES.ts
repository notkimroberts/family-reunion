/* Youth cuts, offered alongside the adult sizes because a family reunion registers children.
   Kept separate from ADULT_SHIRT_SIZES so the picker can label the two groups — "YS" beside "S"
   with no heading reads as a typo. */
export const YOUTH_SHIRT_SIZES = ['YS', 'YM', 'YL', 'YXL'] as const
