/* The suggested gift amounts, shown as buttons on /donate and on the home page.

   A constant rather than per-year settings data: it is copy, it is the same every year, and
   changing it is a one-line commit. Nothing filters or reports on it. */
export const DONATION_PRESET_CENTS = [2500, 5000, 10000, 25000]
