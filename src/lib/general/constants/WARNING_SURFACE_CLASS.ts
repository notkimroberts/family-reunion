/* The "wants attention, nothing has gone wrong" surface: a filled amber panel, both schemes set.

   Extracted because a third surface reached for amber and invented its own shades — the registration
   deadline pill, which is the same kind of statement as a closed year's banner: a fact to plan
   around, not an error. EVENT_STATUS_STYLES' own header records that exact drift happening once
   already, across three status surfaces.

   Explicit palette classes rather than semantic tokens because the theme has no amber token, and both
   light and dark are set so dark-mode auto-inversion cannot wash them out. Distinct from the
   destructive variant on purpose: red is a failure, amber is a deadline. */
export const WARNING_SURFACE_CLASS =
    'border-amber-300 bg-amber-50 text-amber-900 dark:border-amber-800 dark:bg-amber-950 dark:text-amber-200'
