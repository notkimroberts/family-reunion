import type { RegistrationCategory } from '$lib/types/registrationCategory'

export const REGISTRATION_CATEGORY_LABELS: Record<RegistrationCategory, string> = {
    adult: 'Adult',
    child: 'Child',
}
